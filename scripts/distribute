#!/usr/bin/env node

const { gitAddCommitPush } = require('@sifrr/dev');
gitAddCommitPush({
  preCommand: ['sh ./scripts/release/config', 'yarn build', 'sh ./scripts/contributors'],
  files: ['*dist/*.js*', '*package.json', 'yarn.lock', '*rollup.config.js', 'misc/*'],
  commitMsg: 'chore: add auto generated files'
});
